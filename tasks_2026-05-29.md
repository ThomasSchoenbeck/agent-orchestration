# Tasks — 2026-05-29

## Task 1 — Task detail page: branch, files and commits panel

**Status:** `[x]` done

### Goal
The task detail page should show what the agent actually did: which branch it worked on, the files in that branch, and the commit history on that branch.

### What to build
Add a "Code" tab (or section) to TaskDetail.svelte that shows:
1. **Branch name** — derived from the task id: `task/<task.id>`. Display it as a copyable label.
2. **File tree** — reuse the existing `FileTree` component, passing `project_id` from the task and `ref=task/<task.id>`. Only show this panel if the task has a `project_id`.
3. **Commit log** — a list of commits on the branch (author, short SHA, message, date). Needs a new backend endpoint and git function.

### Backend work needed
- **`git.ListCommits(repoPath, ref string) ([]CommitEntry, error)`** — new function in `git/reader.go`. Returns commits on `ref` in reverse-chronological order (limit ~50). `CommitEntry` has: `SHA`, `ShortSHA`, `Message`, `AuthorName`, `AuthorEmail`, `Date`.
- **`GET /api/projects/{id}/commits?ref=<branch>`** — new route in `handlers.go` calling `git.ListCommits`. Returns empty array (not 404) when ref doesn't exist.
- Register the route under `handleProjectDetail` alongside `tree`, `file`, `diff`.

### Frontend work needed
- **`api.js`** — add `listCommits(projectId, ref)` calling `/api/projects/${projectId}/commits?ref=${ref}`.
- **`TaskDetail.svelte`** — add a "Code" tab alongside the existing tabs. Inside it:
  - Show `task/<task.id>` as the branch label (always, when task has project_id)
  - Show `<FileTree>` for the branch
  - Show commit list below the file tree (or alongside it)
  - Only render when `task.project_id` is set
- The tab should be inactive (greyed) if there's no project_id on the task.

### Files to touch
- `git/reader.go` — add `ListCommits`
- `git/reader_test.go` — add `TestListCommits_*`
- `server/handlers.go` — add `handleProjectCommits`, register route
- `server/git_routes_test.go` — add `TestProjectCommits_*`
- `ui/src/lib/api.js` — add `listCommits`
- `ui/src/pages/TaskDetail.svelte` — add Code tab

### Tests
- `TestListCommits_EmptyRepo` — expect `[]`, no error
- `TestListCommits_NonexistentRef` — expect `[]`, no error (same as tree endpoint)
- `TestListCommits_AfterCommit` — commit a file, verify commit appears in list with correct SHA/message
- `TestProjectCommits_EmptyRepo` — GET returns 200 `[]`
- `TestProjectCommits_WithCommits` — commit via PUT /file, then GET commits returns the commit

---

## Task 2 — Project detail file/diff view: branch selector with file tree and commit log

**Status:** `[x]` done

### Goal
The Files tab on the project detail page needs a proper branch-aware view: a branch list on the left, a file tree for the selected branch, and a commit log for that branch. The current layout has the file tree embedded but no commit visibility.

### What to build
Replace the current Files tab layout with a three-panel design (or two-panel with a toggle):

**Left column** — branch list
- Fetched via `listBranches(projectId)`
- Clicking a branch selects it and refreshes the file tree + commit log
- Currently selected branch is highlighted
- `main` is selected by default

**Middle column** — file tree
- Already exists via `FileTree` component
- Drive `selectedRef` from the selected branch in the left panel
- (FileTree already has its own internal branch selector — remove or hide it since the project page now drives the branch externally via a prop)

**Right column / bottom panel** — commit log for selected branch
- Uses the new `listCommits(projectId, ref)` API from Task 1
- Shows: short SHA (monospace, copyable), commit message, author, relative date
- Empty state: "No commits on this branch yet"

### Changes to FileTree component
- Accept an optional `ref` prop; if provided, use it instead of the internal branch selector
- When `ref` prop is provided, hide the internal `<select>` branch dropdown (the parent drives it)
- Keep internal branch selector as fallback when no `ref` prop is given (backward compat for other uses)

### Layout sketch
```
[ Files tab active ]

┌─ Branches ──────┬─ Files ────────────────┬─ Commits ─────────────┐
│ ● main          │ 📁 src/                 │ abc1234  Add README    │
│   task/abc12…   │   📄 main.go            │          agent  2m ago │
│                 │ 📄 README.md            │                        │
└─────────────────┴─────────────────────────┴────────────────────────┘
```

On narrow screens: stack vertically (branches on top, tree middle, commits bottom).

### Diff tab
- Keep the existing diff tab as-is, but pre-populate `diffHeadRef` with the currently selected branch when the user switches to the Diff tab.

### Files to touch
- `ui/src/components/FileTree.svelte` — accept optional `ref` prop; hide internal branch selector when prop is set
- `ui/src/pages/ProjectDetail.svelte` — restructure Files tab with branch list + commit log panels; wire selected branch into FileTree and commit log
- `ui/src/lib/api.js` — `listCommits` already added in Task 1

### Tests
- FileTree: when `ref` prop is passed, internal branch selector is not rendered
- FileTree: when no `ref` prop, internal branch selector renders as before
- ProjectDetail: selecting a branch in the branch list updates the file tree ref
- ProjectDetail: commit log shows commits for the selected branch

---

## Task 3 — Fix agent execution pipeline: logging, LLM visibility, review flow, and push

**Status:** `[x]` done

### How the colocated workflow actually works (context)
In colocated mode the **server** — not the agent — provisions the worktree. During `ClaimTask`, the server calls `CreateWorktree` which clones the project's bare repo into a local directory and checks out the task branch. It also writes `.agent_context/` context files (task description, project rules, last review). The claim response returns `worktree_path`; the agent receives this path and works inside it directly. The agent never clones anything itself. This entire setup is currently invisible to the user — nothing is logged to the task.

### Root cause summary

**Gap 1 — Routing fails silently before the LLM is ever called.**
`execute()` tries `RouteByTaskType(task.Type)` then `RouteByRole(task.Role)`. Both failures are logged at `Debug` level only — never shipped to the server. If the role in the DB has no `provider_id` set (common when the role was seeded or created without explicitly linking a provider), `routeFromCache` fails. The config-file fallback also typically fails. The provider role-preference fallback (`GetForRole`) only works if the provider's `roles` list was set at agent startup. When all three fail, `execute()` returns an error immediately — the LLM is never contacted — and the only record of this is an `Error`-level log that the user must know to look for in the logs tab. This is the confirmed reason the local LLM receives no calls.

**Gap 2 — Worktree provisioning never logged to the task.**
`provisionColocatedWorktree` logs to server stdout only. No task comment, no task log entry. User sees the directory appear on disk with no corresponding event in the UI.

**Gap 3 — Review step completely skipped.**
After a worker task's LLM run, `Run()` calls `SubmitTaskResult(TaskStatusCompleted)`. `TaskStatusAwaitingReview` is defined but never used. `client.SubmitForReview()` exists but is never called. The correct sequence after a worker finishes: commit files → push branch to the project repo → post a comment saying which branch was pushed → call `SubmitForReview` to hand off to a reviewer agent.

**Gap 4 — ZeroHash worktree has no remote; push always fails.**
When `CreateWorktree` encounters a ZeroHash base (repo had no commits at claim time), it falls back to `gogit.PlainInit` without configuring a remote. `CommitAndPush` with an empty `remoteURL` then fails because there is no "origin". This is a push to the bare repo, which IS the server's repo. Fix: after `PlainInit`, call `repo.CreateRemote("origin", repoPath)` so the push has a destination. New projects now always have an initial commit on main (from earlier fix), so this path is rarer — but the code remains broken for any project that was initialized before that fix.

**Gap 5 — `projectID` missing from task logger.**
Log entries are tagged with `task_id` and `agent_id` but not `project_id`. Project-scoped log views in the UI receive no agent log entries.

**Gap 6 — Routing not validated at agent startup.**
Agent logs "executor wired (LLM providers: N)" at startup but never checks whether each of its roles actually resolves to a provider. A misconfigured role (no `provider_id`, no config mapping, no provider role-preference) stays silent until a task is claimed and fails. Users have no way to know the agent is misconfigured until they look at a failed task's log.

### Changes required

#### `agent/executor.go`
- Promote routing attempt logs to Info: `tlog.InfoCtx(ctx, "routing task type=%q role=%q", task.Type, task.Role)` before the route calls; `tlog.InfoCtx(ctx, "route resolved provider=%q model=%q", ...)` on success; `tlog.ErrorCtx(ctx, "routing failed: %v", err)` on total failure (currently only returned as an error, not logged at the call site).
- Promote LLM call logs to Info: `tlog.InfoCtx(ctx, "calling LLM provider=%q model=%q round=%d", ...)` and `tlog.InfoCtx(ctx, "LLM response stop=%s tool_calls=%d tokens=%d", ...)`.
- After `commitTaskWork` produces a new commit SHA: post a comment "Branch `task/<id>` pushed to project repo. Commit: `<sha12>`." then call `e.client.SubmitForReview(ctx, task.ID)` instead of `SubmitTaskResult(Completed)`.
- If worktree is clean after LLM run (nothing committed): submit `TaskStatusCompleted` with result note "No file changes produced."
- Set project ID on logger at start of `Run()`: derive from `task.ProjectID`.

#### `agent/agent.go`
- In `buildAgent`, after `rtr.LoadFromDB`, call `rtr.RouteByRole(role)` for each agent role and log at Info: `"role %q resolves to provider=%q model=%q"` (success) or Warn: `"role %q has no route — tasks for this role will fail immediately"` (failure).

#### `server/claim.go` — `provisionColocatedWorktree`
- After `CreateWorktree` succeeds, create a task log entry: `"Worktree provisioned at <path> on branch task/<taskID> (base: main)"`. Level info, associated with `agentID` and `taskID`.
- Post a task comment from the server: `"Worktree ready on branch \`task/<taskID>\`. Agent is starting."` This appears before the agent's own start comment.

#### `git/worktree.go` — ZeroHash path
- After `gogit.PlainInit(worktreePath)`, open the new repo and call `CreateRemote("origin", repoPath)` so `CommitAndPush` can push back to the bare repo.

### Files to touch
- `agent/executor.go`
- `agent/agent.go`
- `server/claim.go`
- `git/worktree.go`
- `agent/executor_test.go`
- `git/worktree_test.go`

### Tests

#### `executor_test.go`
- `TestRun_LogsRoutingAttemptAtInfoLevel` — mock router success; verify Info log containing "routing task" is posted to the server before any LLM call.
- `TestRun_LogsRoutingFailureAtErrorLevel` — mock router failure; verify Error log containing "routing failed" is posted and `SubmitTaskResult(Failed)` is called.
- `TestRun_LogsLLMCallAtInfoLevel` — mock LLM; verify Info log containing "calling LLM" appears.
- `TestRun_WorkerWithCommitSubmitsForReview` — mock LLM + file write that produces a commit; verify `SubmitForReview` is called and NOT `SubmitTaskResult(Completed)`.
- `TestRun_WorkerCleanWorktreeSubmitsCompleted` — mock LLM, no file changes; verify `SubmitTaskResult(Completed)` is called, `SubmitForReview` is not.
- `TestRun_PostsBranchPushedComment` — mock LLM + commit; verify a comment containing the branch name and commit SHA is posted before `SubmitForReview`.
- `TestRun_ProjectIDOnLogger` — verify log entries have `project_id` populated from `task.ProjectID`.

#### `git/worktree_test.go`
- `TestCreateWorktree_ZeroHashHasOriginRemote` — bare repo with no commits → `CreateWorktree` → open resulting repo → verify remote "origin" is configured pointing at bare repo path.
- `TestCreateWorktree_ZeroHashCanCommitAndPush` — extend above: write a file, commit, push → verify commit appears in the bare repo's object store.

#### Manual end-to-end test steps
1. Create a project and init repo (main branch created with initial commit).
2. Ensure the agent's role has a provider linked (either via role form with `provider_id`, config `roles:` mapping, or provider `roles` list). Start agent — verify log says `"role %q resolves to provider=%q model=%q"`.
3. Queue a task assigned to that role.
4. Agent claims task → verify task shows a comment "Worktree ready on branch `task/<id>`."
5. Verify task log shows "routing task type=... role=..." and then "calling LLM provider=... model=...".
6. After LLM finishes → verify task log shows "LLM response stop=end_turn...".
7. If agent wrote files → verify task comment "Branch `task/<id>` pushed to project repo. Commit: `<sha>`."
8. Verify task status changes to `AWAITING_REVIEW`.
9. Start a reviewer agent → verify it picks up the task, posts a review, and transitions to `COMPLETED` or `AWAITING_REVISION`.




Open Tasks
- Tool call errors:
  >  tool call: write_file  
  >  result: {"error":"missing required argument \"repo_path\""}  
- task detail page should not only show the branch but also show the files
- add a button to delete task events
