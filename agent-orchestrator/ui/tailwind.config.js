// Tailwind v4: theme configuration has moved to CSS (@theme block in app.css).
// This file is intentionally empty — v4 no longer uses JS-based theme config.
// See src/app.css for colour definitions.
export default {}
