// Package router: prompt templating (the config.Prompts-backed Prompter) was
// removed together with the task-type subsystem. System prompts now come from
// each role definition's system_prompt field (see RouteResult.SystemPrompt).
package router
